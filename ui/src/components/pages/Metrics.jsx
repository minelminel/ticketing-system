import React from 'react';
import CanvasJSReact from 'canvasjs';

const defaultProps = {};

export default function Metrics(props) {
  return <div>metrics page!</div>;
}

Metrics.defaultProps = defaultProps;
